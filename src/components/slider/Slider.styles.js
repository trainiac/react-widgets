import styles from 'styles'

export default styles.add({
  container: {
    position: 'relative',
    display: 'block',
    height: 8,
    backgroundColor: '#eaeae8',
    border: '1px solid #dadad8'
  },
  range: {
    backgroundColor: '#a7c23d',
    position: 'absolute',
    height: '100%',
    top: 0,
    right: 0,
    left: 0
  }
}).get()
